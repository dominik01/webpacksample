require([
    'cookie',
    'lodash',
    'pubsub',
    'lib/youtube-iframe-api',
    'lib/conditional-loader'
], function () {
    var pubsub = require('pubsub');
    console.log('i am here');

});

// Add tracking
require(['lib/tracking'], function() {
});


console.log('first test');